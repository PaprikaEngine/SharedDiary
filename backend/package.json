{
  "name": "shared-diary-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "cargo install -q worker-build && worker-build --release"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
