# 交換日記サービス — CLAUDE.md

## プロジェクト概要

小中学生の頃に友達とやっていた交換日記を、大人になった今また楽しめるサービス。

テキストだけでなく、画像・動画・パラパラアニメ・動くスタンプで「あの頃の創作の楽しさ」をデジタルで再現する。競合が「家族の連絡ツール」や「カップルアプリ」に寄っているのに対して、**懐かしさ × 創作の楽しさ**で差別化する。

> “また誰かと交換日記がしたい” という気持ちに応えるサービス。

-----

## コアコンセプト

- **非同期・番交代制**: 前の人が書いたら次の人に「バトン」が渡る
- **プライベートな空間**: 招待制グループのみ閲覧可能
- **シンプルさ優先**: 機能を絞り、書くことに集中できるUX

-----

## 主要機能（MVP）

|機能      |説明                                         |
|--------|-------------------------------------------|
|グループ作成  |日記帳を作り、招待URLで仲間を招く                         |
|日記投稿    |手書き（Canvas）がメイン + テキストボックス（任意）+ メディア複数枚 + スタンプ。バトンが自分に来たら書ける|
|メディア添付  |画像（JPEG/PNG/GIF）・動画（MP4/WebM）を複数枚貼り付け可能    |
|パラパラアニメ |うごくメモ帳風のコマ描画エディタ。フレームをめくるアニメを作れる           |
|動くスタンプ  |Lottie / APNG / WebP アニメーションのスタンプをページ上に自由配置|
|スタンプエディタ|スタンプの位置・サイズ・回転を自由に編集できるキャンバス               |
|バトン管理   |誰の番かを明示。期限（例: 3日）を設定可能                     |
|通知      |バトンが来たらメール or プッシュ通知                       |
|リアクション  |動くスタンプで返事できる。テキストコメントなし                    |
|タイムライン  |グループメンバー全員の投稿を時系列で表示                       |

-----

## 技術スタック（想定）

```
# フロントエンド
Framework  : Next.js (App Router) + TypeScript
Styling    : Tailwind CSS
Hosting    : Cloudflare Pages (Free)

# バックエンド
Language   : Rust + Axum
Hosting    : Cloudflare Workers (Free: 100,000 req/day)

# DB / 認証
DB         : Supabase Free (500MB DB, 1GB Storage, 50,000 MAU)

# ストレージ / メディア配信
オブジェクト  : Cloudflare R2 (Free: 10GB storage, 10M reads/month)
画像変換     : 自前実装（Cloudflare Images は有料のため不使用）
CDN        : Cloudflare CDN（静的アセット配信）

# 通知
メール      : Resend (Free: 100 emails/day)
Push通知    : Web Push (無料)

# メディア・アニメーション関連ライブラリ
描画キャンバス  : Fabric.js（スタンプ配置・変形）
パラパラ描画    : Canvas API（軽量・フレーム管理）
アニメスタンプ  : Lottie-web（JSON アニメ）+ APNG / animated WebP
画像圧縮        : browser-image-compression（アップロード前にクライアント圧縮）
```

### 無料枠の制約

| サービス | 無料枠 | 制約 |
|---------|-------|------|
| Cloudflare Pages | 無制限 | 500ビルド/月、1ビルド20分まで |
| Cloudflare Workers | 100,000 req/day | 10ms CPU時間/リクエスト |
| Cloudflare R2 | 10GB | 10M Class A ops, 10M Class B ops/月 |
| Supabase | 500MB DB | 1GB Storage, 2GB bandwidth/月 |
| Resend | 100通/日 | 3,000通/月 |

**注意**: Cloudflare Stream / Images は有料のため Phase 1 では不使用。
動画は Phase 2 以降で検討、画像最適化はクライアント側圧縮で対応。

## Rust / Supabase / Cloudflare 役割分担

単純なCRUDはSupabase PostgRESTに任せ、Rustはビジネスロジックと重い処理に絞る。
ストレージはCloudflare R2を使用。

|処理                  |担当                         |
|--------------------|---------------------------|
|認証・JWT管理            |Supabase Auth              |
|日記・スタンプ等のCRUD       |Supabase PostgREST         |
|バトン管理・状態遷移          |Rust (Workers)             |
|R2アップロード署名URL発行     |Rust (Workers)             |
|パラパラアニメ合成（フレーム→WebP）|Rust (Workers)             |
|通知トリガー（バトンパス時）      |Rust (Workers)             |
|画像圧縮・リサイズ           |クライアント側 (browser-image-compression)|
|静的アセット配信            |Cloudflare CDN + R2        |

-----

## データモデル（概略）

```
users             id, name, avatar_url, email
groups            id, name, cover_image, created_by, baton_deadline_days,
                  current_baton_holder_id (次に書く人)
group_members     group_id, user_id, role(owner|member), joined_at

entries           id, group_id, author_id, body(nullable), created_at

# メディア添付（1エントリに複数）
entry_media       id, entry_id, type(image|video|animation), url, order, width, height

# パラパラアニメ
flipbooks         id, entry_id, fps, loop
flipbook_frames   id, flipbook_id, order, canvas_json (Fabric.js シリアライズ)

# スタンプ（ページ上に自由配置）
entry_stamps      id, entry_id, stamp_id, x, y, scale, rotation
stamps            id, name, type(lottie|apng|webp), url, thumbnail_url,
                  scope(builtin|group), group_id(nullable), created_by
                  # scope=builtin → 全員利用可、group_id=null
                  # scope=group   → そのグループメンバー全員が共有・利用可能

reactions         entry_id, user_id, stamp_id (動くスタンプでリアクション)
# コメントはテキスト不可のためなし
```

-----

## UX原則

1. **ノートに書く体験を再現** — 日記の入力は手書き（Canvas描画）が基本。テキストを入れたい場合はキャンバス上にテキストボックスを配置する。見た目も操作感も「本物のノートに書いている」感覚を最優先する
1. **表現のハードルを下げる** — テキストは任意。画像・動画・スタンプだけでも成立するが、書きたければ書ける
1. **バトンを見える化** — 誰の番かをトップに常に表示
1. **アナログな質感** — 手書きキャンバス・罫線・紙テクスチャなどで本物のノートを想起させる
1. **プレッシャーを減らす** — 「まだ書いてないよ」ではなく「楽しみに待ってるよ」トーンの通知文
1. **遊びを大事に** — アニメスタンプ・パラパラは「おまけ」ではなく体験の中心。使いたくなるUIを優先
1. **重くしない** — 動画・アニメが多くても快適に。遅延ロード・サムネイル先行表示を徹底

-----

## デザイン方針

- **トーン**: 温かみのある・手書き風・やや懐かしい
- **カラー**: クリーム / モスグリーン / インクブラック をベースに
- **フォント**: 日本語は「Noto Serif JP」、英数字は「Lora」など衬線系
- **アニメーション**: ページめくり演出、バトン受け渡しのふわっとした遷移
- **日記エディタ**: Canvas ベースの手書きキャンバスが主役。ノートの罫線・紙の質感を背景に敷き、ペン・消しゴム・色選択を提供。テキストはキャンバス上にテキストボックスとして配置（通常のフォーム入力ではない）

-----

## フェーズ計画

### Phase 1 — MVP（テキスト + 静止画）

- [ ] 認証（メール + Google OAuth）
- [ ] グループ作成・招待
- [ ] 日記投稿（テキスト任意 + 画像複数枚）
- [ ] バトン管理（手動パス）
- [ ] タイムライン表示

### Phase 2 — リッチメディア

- [ ] 動画添付（MP4/WebM、最大 100MB）
- [ ] アニメーションスタンプ（Lottie / APNG）の配置エディタ
- [ ] 組み込みスタンプセット（うごくメモ帳風 20種〜）
- [ ] メール通知・Web Push
- [ ] リアクション（動くスタンプのみ）

### Phase 3 — パラパラアニメ & 仕上げ

- [ ] パラパラアニメエディタ（Canvas API、フレーム管理、オニオンスキン）
- [ ] ユーザー自作スタンプのアップロード（グループ内で全員共有）
- [ ] バトン期限・リマインダー自動化
- [ ] 日記帳のアーカイブ・PDF / 動画書き出し
- [ ] モバイルアプリ（PWA or React Native）

-----

## 残件・未実装

### マスキングテープ機能 (Phase 1 完了)

組み込み6種(チェック × 3色 / 無地 × 3色)はキャンバスツールバーから選択して2点間に貼れる。
Phase 2(ユーザがグループにテープ画像をアップロード)はまだ未実装。

旧設計メモ(参考):

ノート上に「貼る」装飾要素として、マスキングテープを追加する。

- **インタラクション**: クリックした地点から離した地点までを直線でつなぎ、その2点間にテープ画像を repeat（ループ）して描画
- **アセット**: スタンプと同じく builtin / group スコープで管理。ユーザがグループにテープ画像をアップロードできる
- **デフォルト6種**: チェック柄 × 3色、無地 × 3色（合計6個）を組み込みで用意
- **データモデル案**: `TapeElement = { type: "tape", x1, y1, x2, y2, tapeId }` を新設。`stamps` テーブルとは別の `tapes` テーブルに分けるのが素直（2点保持と1点保持で性質が違う）
- **描画**: テープ画像を `CanvasPattern` で repeat 化し、`translate` + `rotate` した座標系に `fillRect(0, 0, length, tapeHeight)` で描く。半透明（α≒0.85）で紙に貼った質感を出す
- **UI参考**: `frontend/src/components/stamps/*` のスタンプ選択・配置の構造を踏襲

実装は Phase 1 として「フロント完結 + デフォルト6種ハードコード」、Phase 2 で「ユーザアップロード対応」の段階的進行を想定。

-----

## 開発ルール

- コンポーネントは `src/components/` に機能単位でまとめる
- Server Component を基本とし、インタラクションが必要な箇所のみ `"use client"`
- 型は `src/types/` に集約し、DB スキーマと一致させる
- 環境変数は `.env.local`（本番は Cloudflare Pages / Workers の環境変数設定）
- Supabase の RLS（Row Level Security）を必ず有効にする

### メディア・アニメーション固有ルール

|項目      |制約                                                    |
|--------|------------------------------------------------------|
|画像      |アップロード前に `browser-image-compression` で 2MB 以下に圧縮      |
|動画      |最大 100MB・最長 3分。超過時はアップロード拒否しユーザーに案内                   |
|Lottie  |JSON ファイルのみ許可。外部 CDN 参照は禁止（セキュリティ）                    |
|パラパラフレーム|最大 60フレーム・1フレームあたり Canvas 解像度 800×600px               |
|スタンプ配置  |1ページあたり最大 20個（パフォーマンス上限）                              |
|ストレージパス |R2: `{group_id}/{entry_id}/{type}/{filename}` で統一   |

-----

## 競合・差別化

### 主な競合

|アプリ    |特徴              |弱点                    |
|-------|----------------|----------------------|
|家族ダイアリー|写真・気分アイコン・AIコメント|テキスト中心、動画・アニメなし       |
|Shanfy |写真4枚・リアクション     |2024年に仕様大幅変更→旧ユーザー大量離脱|
|Waffle |カップル・家族向け共有日記   |シンプルすぎ、リッチメディアなし      |
|瞬間日記   |テキスト・写真・動画・ボイスメモ|個人日記メイン、交換日記は補助機能     |

### 差別化ポイント

- **動画・パラパラアニメ・動くスタンプ**を持つ交換日記サービスは現時点で存在しない
- 競合は「連絡・記録ツール」寄り。このサービスは**創作・遊び**が体験の中心
- Shanfy離脱ユーザーに見られるように、バトン型の純粋な交換日記への需要は根強い

### ターゲット

小中学生時代に交換日記をやっていた**20〜30代**。あの頃の感覚をもう一度味わいたい人。

-----

|用語     |意味                                          |
|-------|--------------------------------------------|
|日記帳    |1つのグループ = 1冊の交換日記                           |
|バトン    |次に書く権利・順番                                   |
|ページ    |1件の日記エントリ                                   |
|メンバー   |日記帳に参加しているユーザー                              |
|パラパラ   |うごくメモ帳風のコマ送りアニメーション。1ページに1つ添付可能             |
|動くスタンプ |Lottie / APNG / animated WebP 形式のアニメーションスタンプ|
|スタンプ配置 |ページの任意座標にスタンプを貼り付けること                       |
|オニオンスキン|パラパラ描画時に前フレームを透かして表示する機能                    |